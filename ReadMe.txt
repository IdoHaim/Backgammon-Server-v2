Data Base:
   1) go to appsettings.json in SignalrServer project and change the DbConnectionSgtring
   2) if you want to create a new data base run the command "update-database" in the Package Manager Console 
   (do it after you have already opened new server and changed the connection string)


Packages:
   SignalrServer:
      bcrypt.net-next\4.0.3
      ...
      ...
      ...
